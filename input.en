
class Friend {
	var name = "missingno";
	var age = 13;

	func init(name) {
		this.name = name;
	}

	func say_hello() {
		print(name + " says hello.");
	}
}

var list = [new Friend("peter"), new Friend("buckle"), new Friend("knopp")];

for (var f in list) {
	print(f.name);
	if (f.name == "peter") {
		print("thats him!");
	}
}
